import { default as React } from 'react';
import { SidebarType } from './types';
declare const Sidebar: React.MemoExoticComponent<({ type, filterOptions, triggerHistory, hasMore, setPagination, fetchIdData, handleViewAllHistory, children, renderRunSource, resourceId, path, }: SidebarType) => import("react/jsx-runtime").JSX.Element>;
export default Sidebar;
