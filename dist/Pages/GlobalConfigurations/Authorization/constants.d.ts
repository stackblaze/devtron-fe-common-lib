export declare enum UserListSortableKeys {
    email = "email_id",
    lastLogin = "last_login"
}
/**
 * User Status received from the API
 */
export declare enum UserStatusDto {
    active = "active",
    inactive = "inactive"
}
/**
 * User status for the frontend
 */
export declare enum UserStatus {
    active = "active",
    inactive = "inactive",
    temporary = "temporaryAccess"
}
